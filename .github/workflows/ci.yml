# PR / merge gate aligned with docs/VERIFICATION.md (chronon-backend + teaching
# host + chronon-app SSR tests + chronon-uf-app-e2e boundary + Playwright).
# Path deps assume a Unified Field sibling checkout layout; CI places this repo
# under that path and shallow-clones siblings.
name: CI

on:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  CARGO_BUILD_JOBS: 1
  CARGO_TARGET_DIR: target-chronon-uf-app
  RUST_BACKTRACE: 1
  # Leptos workspace deps enable the `nightly` feature.
  RUSTFLAGS: "-D warnings"
  UF_ROOT: unified-field
  APP_WD: unified-field/L4-composers/chronon-uf-app

jobs:
  fmt:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout chronon-uf-app into unified-field layout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          path: ${{ env.APP_WD }}
      - name: Checkout path-dep / patch siblings
        run: |
          set -euo pipefail
          clone() {
            local repo="$1" dest="$2"
            git clone --depth 1 "https://github.com/deathbreakfast/${repo}.git" "${dest}"
          }
          clone valence "${UF_ROOT}/L0-upstream-cores/valence"
          clone orbital "${UF_ROOT}/L0-upstream-cores/orbital"
          clone chronon "${UF_ROOT}/L0-upstream-cores/chronon"
          clone photon "${UF_ROOT}/L0-upstream-cores/photon"
          clone boson "${UF_ROOT}/L0-upstream-cores/boson"
          clone higgs "${UF_ROOT}/L1-host-stack-kits/higgs"
          clone lepton "${UF_ROOT}/L1-host-stack-kits/lepton"
          clone chronon-coordinator-macros "${UF_ROOT}/L1-host-stack-kits/chronon-coordinator-macros"
          clone chronon-valence-identity "${UF_ROOT}/L1-host-stack-kits/chronon-valence-identity"
          clone boson-valence-identity "${UF_ROOT}/L1-host-stack-kits/boson-valence-identity"
          clone photon-leptos "${UF_ROOT}/L1-host-stack-kits/photon-leptos"
          clone unified-field-product "${UF_ROOT}/L2-product-platform/unified-field-product"
          clone uf-notifications "${UF_ROOT}/L2-product-platform/uf-notifications"
          # Workspace member chronon-app path-depends on lepton-shell.
          clone lepton-uf-app "${UF_ROOT}/L2-product-platform/lepton-uf-app"
          # Workspace deps: gauge (ssr optional) + Higgs (path deps).
          clone gauge "${UF_ROOT}/L2-product-platform/gauge"
      - uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c # master (pin)
        with:
          toolchain: nightly
          components: rustfmt
      - name: cargo fmt --check
        working-directory: ${{ env.APP_WD }}
        run: >
          cargo fmt -p chronon-backend -p chronon-app -p protected-chronon-host
          -p chronon-uf-app-e2e -- --check

  clippy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout chronon-uf-app into unified-field layout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          path: ${{ env.APP_WD }}
      - name: Checkout path-dep / patch siblings
        run: |
          set -euo pipefail
          clone() {
            local repo="$1" dest="$2"
            git clone --depth 1 "https://github.com/deathbreakfast/${repo}.git" "${dest}"
          }
          clone valence "${UF_ROOT}/L0-upstream-cores/valence"
          clone orbital "${UF_ROOT}/L0-upstream-cores/orbital"
          clone chronon "${UF_ROOT}/L0-upstream-cores/chronon"
          clone photon "${UF_ROOT}/L0-upstream-cores/photon"
          clone boson "${UF_ROOT}/L0-upstream-cores/boson"
          clone higgs "${UF_ROOT}/L1-host-stack-kits/higgs"
          clone lepton "${UF_ROOT}/L1-host-stack-kits/lepton"
          clone chronon-coordinator-macros "${UF_ROOT}/L1-host-stack-kits/chronon-coordinator-macros"
          clone chronon-valence-identity "${UF_ROOT}/L1-host-stack-kits/chronon-valence-identity"
          clone boson-valence-identity "${UF_ROOT}/L1-host-stack-kits/boson-valence-identity"
          clone photon-leptos "${UF_ROOT}/L1-host-stack-kits/photon-leptos"
          clone unified-field-product "${UF_ROOT}/L2-product-platform/unified-field-product"
          clone uf-notifications "${UF_ROOT}/L2-product-platform/uf-notifications"
          clone lepton-uf-app "${UF_ROOT}/L2-product-platform/lepton-uf-app"
          clone gauge "${UF_ROOT}/L2-product-platform/gauge"
      - uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c # master (pin)
        with:
          toolchain: nightly
          components: clippy
      - name: cargo clippy (backend + app SSR + teaching host)
        working-directory: ${{ env.APP_WD }}
        run: |
          cargo clippy -p chronon-backend --all-targets -- -D warnings
          cargo clippy -p protected-chronon-host --all-targets -- -D warnings
          # chronon-app still carries pedantic+nursery warn debt; keep unwrap/expect deny
          # while RUSTFLAGS=-D warnings would otherwise fail the UI graph on style lints.
          # chronon-uf-app-e2e is a lab harness (expect-heavy); skip clippy there.
          cargo clippy -p chronon-app --features ssr --all-targets -- \
            -D warnings -A clippy::pedantic -A clippy::nursery

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout chronon-uf-app into unified-field layout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          path: ${{ env.APP_WD }}
      - name: Checkout path-dep / patch siblings
        run: |
          set -euo pipefail
          clone() {
            local repo="$1" dest="$2"
            git clone --depth 1 "https://github.com/deathbreakfast/${repo}.git" "${dest}"
          }
          clone valence "${UF_ROOT}/L0-upstream-cores/valence"
          clone orbital "${UF_ROOT}/L0-upstream-cores/orbital"
          clone chronon "${UF_ROOT}/L0-upstream-cores/chronon"
          clone photon "${UF_ROOT}/L0-upstream-cores/photon"
          clone boson "${UF_ROOT}/L0-upstream-cores/boson"
          clone higgs "${UF_ROOT}/L1-host-stack-kits/higgs"
          clone lepton "${UF_ROOT}/L1-host-stack-kits/lepton"
          clone chronon-coordinator-macros "${UF_ROOT}/L1-host-stack-kits/chronon-coordinator-macros"
          clone chronon-valence-identity "${UF_ROOT}/L1-host-stack-kits/chronon-valence-identity"
          clone boson-valence-identity "${UF_ROOT}/L1-host-stack-kits/boson-valence-identity"
          clone photon-leptos "${UF_ROOT}/L1-host-stack-kits/photon-leptos"
          clone unified-field-product "${UF_ROOT}/L2-product-platform/unified-field-product"
          clone uf-notifications "${UF_ROOT}/L2-product-platform/uf-notifications"
          clone lepton-uf-app "${UF_ROOT}/L2-product-platform/lepton-uf-app"
          clone gauge "${UF_ROOT}/L2-product-platform/gauge"
      - uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c # master (pin)
        with:
          toolchain: nightly
      - name: cargo test (sibling-source UI contracts)
        working-directory: ${{ env.APP_WD }}
        run: cargo test -p chronon-backend --test workspace_members --test product_surface
      - name: cargo test (chronon-backend contracts)
        working-directory: ${{ env.APP_WD }}
        run: cargo test -p chronon-backend
      - name: cargo test (chronon coordinator boundary)
        working-directory: ${{ env.APP_WD }}
        run: cargo test -p chronon-uf-app-e2e --features ssr --test boundary_contract
      - name: cargo test (chronon-app SSR)
        working-directory: ${{ env.APP_WD }}
        run: cargo test -p chronon-app --features ssr
      - name: cargo check (chronon-app + e2e host SSR)
        working-directory: ${{ env.APP_WD }}
        run: |
          cargo check -p chronon-app --features ssr
          cargo check -p chronon-uf-app-e2e --features ssr
      - name: cargo check + run (protected-chronon-host)
        working-directory: ${{ env.APP_WD }}
        run: |
          cargo check -p protected-chronon-host
          cargo run -p protected-chronon-host

  e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout chronon-uf-app into unified-field layout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          path: ${{ env.APP_WD }}
      - name: Checkout path-dep / patch siblings
        run: |
          set -euo pipefail
          clone() {
            local repo="$1" dest="$2"
            git clone --depth 1 "https://github.com/deathbreakfast/${repo}.git" "${dest}"
          }
          clone valence "${UF_ROOT}/L0-upstream-cores/valence"
          clone orbital "${UF_ROOT}/L0-upstream-cores/orbital"
          clone chronon "${UF_ROOT}/L0-upstream-cores/chronon"
          clone photon "${UF_ROOT}/L0-upstream-cores/photon"
          clone boson "${UF_ROOT}/L0-upstream-cores/boson"
          clone higgs "${UF_ROOT}/L1-host-stack-kits/higgs"
          clone lepton "${UF_ROOT}/L1-host-stack-kits/lepton"
          clone chronon-coordinator-macros "${UF_ROOT}/L1-host-stack-kits/chronon-coordinator-macros"
          clone chronon-valence-identity "${UF_ROOT}/L1-host-stack-kits/chronon-valence-identity"
          clone boson-valence-identity "${UF_ROOT}/L1-host-stack-kits/boson-valence-identity"
          clone photon-leptos "${UF_ROOT}/L1-host-stack-kits/photon-leptos"
          clone unified-field-product "${UF_ROOT}/L2-product-platform/unified-field-product"
          clone uf-notifications "${UF_ROOT}/L2-product-platform/uf-notifications"
          clone lepton-uf-app "${UF_ROOT}/L2-product-platform/lepton-uf-app"
          clone gauge "${UF_ROOT}/L2-product-platform/gauge"
      - uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c # master (pin)
        with:
          toolchain: nightly
          targets: wasm32-unknown-unknown
      - name: Install cargo-leptos
        run: cargo install cargo-leptos --locked --version 0.3.6
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: ${{ env.APP_WD }}/chronon-uf-app-e2e/end2end/package-lock.json
      - name: Install npm dependencies
        working-directory: ${{ env.APP_WD }}/chronon-uf-app-e2e/end2end
        run: npm ci
      - name: Get Playwright version
        id: pw-version
        working-directory: ${{ env.APP_WD }}/chronon-uf-app-e2e/end2end
        run: echo "version=$(node -p "require('./package-lock.json').packages['node_modules/@playwright/test'].version")" >> $GITHUB_OUTPUT
      - uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.pw-version.outputs.version }}-chronon-uf-app
      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        working-directory: ${{ env.APP_WD }}/chronon-uf-app-e2e/end2end
        run: npx playwright install --with-deps chromium
      - name: Install Playwright OS deps when cache hit
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        working-directory: ${{ env.APP_WD }}/chronon-uf-app-e2e/end2end
        run: npx playwright install-deps chromium
      - name: cargo leptos end-to-end
        working-directory: ${{ env.APP_WD }}
        env:
          CI: true
        run: cargo leptos end-to-end --project chronon-uf-app-e2e
      - name: Upload Playwright artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts-chronon-uf-app-${{ github.run_id }}
          path: |
            ${{ env.APP_WD }}/chronon-uf-app-e2e/end2end/playwright-report/
            ${{ env.APP_WD }}/chronon-uf-app-e2e/end2end/test-results/
          if-no-files-found: ignore

  docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout chronon-uf-app into unified-field layout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          path: ${{ env.APP_WD }}
      - name: Checkout path-dep / patch siblings
        run: |
          set -euo pipefail
          clone() {
            local repo="$1" dest="$2"
            git clone --depth 1 "https://github.com/deathbreakfast/${repo}.git" "${dest}"
          }
          clone valence "${UF_ROOT}/L0-upstream-cores/valence"
          clone orbital "${UF_ROOT}/L0-upstream-cores/orbital"
          clone chronon "${UF_ROOT}/L0-upstream-cores/chronon"
          clone photon "${UF_ROOT}/L0-upstream-cores/photon"
          clone boson "${UF_ROOT}/L0-upstream-cores/boson"
          clone higgs "${UF_ROOT}/L1-host-stack-kits/higgs"
          clone lepton "${UF_ROOT}/L1-host-stack-kits/lepton"
          clone chronon-coordinator-macros "${UF_ROOT}/L1-host-stack-kits/chronon-coordinator-macros"
          clone chronon-valence-identity "${UF_ROOT}/L1-host-stack-kits/chronon-valence-identity"
          clone boson-valence-identity "${UF_ROOT}/L1-host-stack-kits/boson-valence-identity"
          clone photon-leptos "${UF_ROOT}/L1-host-stack-kits/photon-leptos"
          clone unified-field-product "${UF_ROOT}/L2-product-platform/unified-field-product"
          clone uf-notifications "${UF_ROOT}/L2-product-platform/uf-notifications"
          clone lepton-uf-app "${UF_ROOT}/L2-product-platform/lepton-uf-app"
          clone gauge "${UF_ROOT}/L2-product-platform/gauge"
      - uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c # master (pin)
        with:
          toolchain: nightly
      - name: cargo doc (chronon-backend, deny broken intra-doc links)
        working-directory: ${{ env.APP_WD }}
        env:
          RUSTDOCFLAGS: "-D rustdoc::broken-intra-doc-links"
        run: cargo doc -p chronon-backend --no-deps
